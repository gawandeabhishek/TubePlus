import { StreamContainer } from "@/components/stream/stream-container"

export default function StreamPage() {
  return (
    <main className="min-h-screen bg-background">
      {/* WIP: landscape mode for mobile view and tablet view and also its now static make it dynamic entire stream page using bolt on account abhishektesting.dev */}
      <StreamContainer />
    </main>
  )
}